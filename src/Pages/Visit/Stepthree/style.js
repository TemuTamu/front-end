import styled from 'styled-components';
import { Container } from 'semantic-ui-react';

const StyledContainer = styled(Container)`
    margin-top: 4em;
`
const StyledParagraph = styled.div`
      text-align: center;
`

const StyledWifiCode = styled.h1`
      text-align: center;
`

export {
  StyledContainer,
  StyledParagraph,
  StyledWifiCode,
};